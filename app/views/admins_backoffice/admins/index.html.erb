<div class="d-flex justify-content-between">
   <h2 class="mb-4"><%= t('messages.listing', resource: @admins.model_name.human(count: 2)) %></h2>
   <%= link_to new_admins_backoffice_admin_path, class: "mt-2 btn btn-success btn-circle btn-sm", title: 'Cadastrar administrador',
   'data-toggle' => 'tooltip' do %>
      <i class="fas fa-plus"></i>
   <% end %>
</div>

<div class="table-responsive">
   <table class="table table-bordered table-hover dataTable" width="100%" cellspacing="0">
   <thead>
      <tr>
      <th>#</th>
      <th><%= translate(@admins, :email)%></th>
      <th>Ações</th>
      </tr>
   </thead>

   <tbody>
      <% @admins.each do |admin| %>
         <tr>
            <td><%= admin.id %></td>
            <td><%= admin.email %></td>
            <td style="width: 100px;">
               <%= link_to edit_admins_backoffice_admin_path(admin), class: "btn btn-primary btn-circle btn-sm" do %>
                  <i class="fas fa-pencil"></i>
               <% end %>
               <%= link_to admins_backoffice_admin_path(admin), method: :delete, 
               data: { confirm: t('messages.confirmation', item: admin.email.truncate(20))}, 
               class: "btn btn-danger btn-circle btn-sm" do %>
                  <i class="fas fa-trash"></i>
               <% end %>  
            </td>
         </tr>
      <% end %>                    
   </tbody>
   </table>
</div>
